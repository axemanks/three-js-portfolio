import ReactDOM from 'react-dom/client';
import App from './App';
import './index.css';

// pass ID of the element to render the app into
ReactDOM.createRoot(document.getElementById('root')).render(<App />);